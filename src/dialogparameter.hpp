#ifndef DIALOGPARAMETER_HPP
#define DIALOGPARAMETER_HPP

#include <QDialog>
#include <QObject>

class DialogParameter : public QDialog
{
    Q_OBJECT

public:
    explicit DialogParameter(QWidget *parent = nullptr);

signals:

};

#endif // DIALOGPARAMETER_HPP
